DATABASE_URL=mongodb://127.0.0.1/payload-template-blank-3-0
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Origin of the Payload admin panel, used to validate Live Preview postMessage events
PAYLOAD_SERVER_URL=http://localhost:3000
# Must match PREVIEW_SECRET in the payload app, gates access to draft content
PREVIEW_SECRET=PREVIEW_SECRET
